@model IEnumerable<WebProgramlamaProje.Models.Employee>

<h2>Çalışanlar</h2>

<table class="table">
    <thead>
        <tr>
            <th>Adı</th>
            <th>Soyadı</th>
            <th>Salon</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var employee in Model)
        {
            <tr>
                <td>@employee.Name</td>
                <td>@employee.Surname</td>
                <td>@employee.Salon?.Name</td>
                <td>
                    <a asp-action="EditEmployee" asp-route-id="@employee.EmployeeId" class="btn btn-primary">Düzenle</a>
                    <form asp-action="DeleteEmployee" method="post" style="display:inline;">
                        <input type="hidden" name="id" value="@employee.EmployeeId" />
                        <button type="submit" class="btn btn-danger" onclick="return confirm('Bu çalışanı silmek istediğinizden emin misiniz?');">Sil</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

<a asp-action="AddEmployee" class="btn btn-success">Yeni Çalışan Ekle</a>
